# 03_模块优先级清单

> **仓库**: open_vins | **代码版本**: `8adae7c` (分支 `study/vio`, `DIRTY`)
> **生成日期**: 2026-08-19
> **依赖**: `01_拓扑结构分析.md` (Phase 1) + `02_数据流追踪.md` (Phase 2)
> **分析者**: Gavin + AI

> **判定依据**: 按 `phase3-priority.md` 的 P0–P3 关键词表对 `ov_*` 各模块做 grep 命中统计 + 代码行数 + 数据流位置综合评定。关键词 grep 命中点见附录 A。

> **⚠️ 编号约定（务必先读）**：本清单 §7 的 `S1…S17 + F1…F3` 是**函数级精读锚点编号**，与 `04/` 目录下的文档文件名前缀 **一一对应**（每个 S 锚点对应一篇独立文档，**不得合并**）。已落地文档映射如下，写新文档时文件名须沿用此编号：
> | 规划锚点 | 函数 | 04/ 文档名 | 状态 |
> |---|---|---|---|
> | S1 | Type 基类 | `S1_Type-base-class-deep-dive.md` | ✅ |
> | S2 | JPLQuat | `S2_JPLQuat-deep-dive.md` | ✅ |
> | S3 | PoseJPL | `S3_PoseJPL-composite-deep-dive.md` | ✅ |
> | S4 | IMU / Vec / Landmark | `S4_IMU-Vec-composite-deep-dive.md` | ✅ |
> | S5 | Propagator::propagate_and_clone | `S5_Propagator-deep-dive.md` | ✅ |
> | S6 | Propagator::select_imu_readings | `S6_Propagator-select_imu_readings-deep-dive.md` | ✅ |
> | S7 | Propagator::predict_and_compute | `S7_Propagator-predict_and_compute-deep-dive.md` | ✅ |
> | S8 | 积分路径(离散/RK4/ACI2 + F/G) | `S8_Propagator-integration-paths-deep-dive.md` | ✅ |
> | S9 | StateHelper::EKFPropagation | `S9_StateHelper-deep-dive.md` | ✅ |
> | S10 | StateHelper::augment_clone + clone | `S10_StateHelper-augment_clone-deep-dive.md` | ✅ |
> | S11 | StateHelper::marginalize / _old_clone / _slam | `S11_StateHelper-marginalize-deep-dive.md` | ✅ |
> | S12 | StateHelper::initialize / _invertible | `S12_StateHelper-initialize-deep-dive.md` | ✅ |
> | S13 | UpdaterMSCKF::update | `S13_UpdaterMSCKF-deep-dive.md` | ✅ |
> | S13a | (子) `get_feature_jacobian_full` | `S13a_*.md` | 待补 (S13 末尾清单) |
> | S13b | (子) `nullspace_project_inplace` | `S13b_*.md` | 待补 (S13 末尾清单) |
> | S13c | (子) `measurement_compress_inplace` | `S13c_*.md` | 待补 (S13 末尾清单) |
> | S14 | UpdaterSLAM::update | （待写） |
> | S15 | UpdaterSLAM::delayed_init | （待写） |
> | S16 | UpdaterZeroVelocity::try_update | （待写） |
> | S17 | VioManager 主循环 | （待写） |
> | F1–F3 | 初始化 + CPI | （待写） |
> > **子文档规则**：主文档（S13）内部调用的核心复杂模块拆为子文档 `S13a/S13b/S13c`（编号 = 主锚点 + 小写字母后缀），归属 `UpdaterHelper`/`FeatureInitializer` 等，详见各主文档末尾"待详细补充项"清单。子文档**不可占用其他主锚点编号**（如 S14 专属于 UpdaterSLAM，不可被 UpdaterMSCKF 的内部模块占用）。规则来源：`slam-code-reader` SKILL.md "04/ 文档拆分规则"。

## 1. 优先级总览图

```
P0 核心数学  ████████████████████  (6 模块, ~12.5k 行)  必须逐行精读 + 公式对照
P1 系统管理  ██████████████        (4 模块, ~9.4k 行)  理解策略逻辑即可
P2 工程辅助  ███████               (2 模块, ~4.8k 行)  了解接口即可
P3 工具/数据 █████                 (4 模块, ~8.1k 行)  用到时查函数签名

█ = ~1k 行（行数仅作体量参考，优先级由算法位置而非体量决定）
```

> 说明：OpenVINS 是算法密集型的滤波式 VIO，P0/P1 占比远高于一般项目（正常，符合 skill 提示"P0 不一定多，但此处核心数学确实多")。

---

## 2. P0 核心模块（必须逐行精读 + 公式对照）

| # | 模块 | 路径 | 关键词命中（代表） | 代码行数 | 预估时间 | 建议顺序 |
|---|------|------|------------------|---------|---------|---------|
| P0-1 | **Propagator（IMU 传播 + 预积分 + 克隆）** | `ov_msckf/src/state/Propagator.*` | `propagate`(34)、`predict_and_compute`、`clone` | 1,996 | 3–4h | 2 |
| P0-2 | **UpdaterMSCKF（多帧投影残差 / 边缘化）** | `ov_msckf/src/update/UpdaterMSCKF.*` | `residual`、`marginal`(12) | 2,309 | 3–4h | 4 |
| P0-3 | **UpdaterSLAM（SLAM 特征 + Aruco 路标）** | `ov_msckf/src/update/UpdaterSLAM.*` | `residual`、`delayed_init`(23) | 2,309 | 3–4h | 5 |
| P0-4 | **StateHelper（克隆增广 / 边缘化 / SLAM 增删）** | `ov_msckf/src/state/StateHelper.*` | `marginal`、`clone`、`augment`(42) | 2,900 | 3–4h | 3 |
| P0-5 | **Type 状态类型体系（FEJ / JPL 四元数）** | `ov_core/src/types/{Type,IMU,PoseJPL,JPLQuat,Vec,Landmark}.*` | `FEJ` 全类型定义、`FirstEstimate` | 1,189 | 2–3h | 1 |
| P0-6 | **初始化器（静态/动态/惯导 + Ceres MLE + CPI）** | `ov_init/src/{static,dynamic,init,ceres}/` + `ov_core/src/cpi/CpiV1.*` | `initialize`(72)、`gravity`、`align`、`optimize`、`CpiV1` | 5,406 + 1,056 | 4–6h | 6 |

> **P0 理由**：P0-1/2/3/4 是 EKF 滤波器的"预测—更新—边缘化—状态管理"四柱，直接对应主论文 MSCKF 滑动窗口与 FEJ 一致性的核心数学；P0-5 是 FEJ/JPL 姿态表示的承载层（citelist [20][21][27][40] 直接落地处）；P0-6 是 `InertialInitializer` 的三种策略 + Ceres MLE 对齐（citelist [11]）。
> **CPI 归属更正（用户指正）**：`ov_core/src/cpi/CpiV1.*` **不是** P3 工具类——它是 **动态初始化（P0-6）的核心实现**：`ov_init/src/dynamic/DynamicInitializer.cpp:30` 直接 `#include "cpi/CpiV1.h"`，逐相机帧构造 `CpiV1` 预积分器（`:243-262`），并经 `ov_init/src/ceres/Factor_ImuCPIv1.*` 接入 Ceres MLE 对齐。故 CPI 归入 P0-6 一并精读（在线滤波器 `Propagator` 确实未 include cpi，仅借鉴其思想，见下 P3-2 注释）。
> **阅读顺序**建议先 P0-5（基础类型）→ P0-1（传播）→ P0-4（状态增删）→ P0-2/3（更新）→ P0-6（初始化 + CPI，可独立成块）。

---

## 3. P1 系统管理模块（理解策略逻辑）

| # | 模块 | 路径 | 关键词命中（代表） | 代码行数 | 预估时间 | 建议顺序 |
|---|------|------|------------------|---------|---------|---------|
| P1-1 | **VioManager（主循环 / 调度）** | `ov_msckf/src/core/VioManager.*` | `feature`、`track`、`update`、`determineStatus` | 1,996 | 1h | 7 |
| P1-2 | **FeatureDatabase（特征生命周期）** | `ov_core/src/feat/{Feature,FeatureDatabase}.*` | `feature`、`add_feature`、`feats_lost/marg/slam` | 1,562 | 0.5h | 8 |
| P1-3 | **TrackKLT（前端特征跟踪）** | `ov_core/src/track/TrackKLT.*` | `track`、`detect`、`matching`、`KeyPoint` | 3,123 | 1h | 9 |
| P1-4 | **UpdaterZeroVelocity（ZUPT 静止检测）** | `ov_msckf/src/update/UpdaterZeroVelocity.*` | `residual`、`try_update`(18)、静止检测 | （含于 update 2,309） | 0.5h | 10 |

> **P1 理由**：这些模块控制"何时做什么"的编排策略（滑动窗口调度、特征分组选取、KLT 跟踪、ZUPT 触发），含少量公式但核心是逻辑流程；理解策略即可，无需逐行推导。P1-1 是串联 P0 各模块的枢纽，建议在 P0 之后通读。

---

## 4. P2 工程辅助模块（扫一眼，了解接口）

| # | 模块 | 路径 | 关键词命中（代表） | 代码行数 | 预估时间 |
|---|------|------|------------------|---------|---------|
| P2-1 | **ROS I/O 与可视化（ROS1/ROS2 Visualizer + Subscriber）** | `ov_msckf/src/ros/{ROS1,ROS2}Visualizer.*`、`ROS1Subscriber.*` | `publish`、`advertise`、`subscribe`、`rviz` | 2,965 | 15min |
| P2-2 | **ov_core/utils（配置/四元数/数学工具）** | `ov_core/src/utils/{config,quat_ops,math_helper,sensor_data}.*` | `param`、`readParam`、`quaternion`、`R2ypr` | 1,882 | 15min |

> **P2 理由**：I/O 边界与参数读取，数据流的进出口（见 Phase 2 §2/§3 回调），理解话题名/参数即可；数学工具（四元数、R2ypr）属"工具类"但被核心频繁调用，故并入 P2 提一眼即可。

---

## 5. P3 工具类 / 数据（按需查函数签名）

| # | 模块 | 路径 | 内容 | 代码行数 |
|---|------|------|------|---------|
| P3-1 | **相机模型** | `ov_core/src/cam/{Camera,CameraRadtan,CameraFishEye}.*` | 投影/反投影、畸变模型 | 632 |
| P3-2 | **连续时间预积分 CPI（离线参考 + 动态初始化实现）** | `ov_core/src/cpi/CpiV1.*` | 连续时间预积分；**被 `ov_init/dynamic` 直接调用（属 P0-6 实现）**，在线 `Propagator` 仅借鉴思想未 include | 1,056 |
| P3-3 | **仿真器 / 绘图** | `ov_core/src/sim/*`、`ov_core/src/plot/*` | 数据合成、结果可视化 | 570 + 1,855 |
| P3-4 | **评估工具集 ov_eval** | `ov_eval/src/{alignment,calc}/*` + 11 个可执行 | 离线轨迹对齐/误差（不参与在线 pipeline） | 5,940 |
| P3-5 | **数据集 ov_data** | `ov_data/<dataset>/*` | 标定/真值 txt，无源码 | — |

> **P3 理由**：相机模型是被 P0 调用的"数学工具"，理解接口足够；sim/plot/eval/data 属离线/辅助，不影响在线算法理解。
> ⚠️ **P3-2 CPI 说明**：CPI（`CpiV1`）具双重身份——① **在线 `Propagator` 未 include cpi**，仅借鉴其连续时间预积分思想自实现（对应 Phase 2 待确认项 #2）；② **动态初始化 `DynamicInitializer` 直接调用 `CpiV1` 并接入 Ceres MLE**（属 P0-6 核心实现，已上移计入 P0-6）。精读 P0-6 时 CPI 必须一并读；精读 P0-1 时若遇预积分公式分歧，可回查 CPI 比对思想来源。

---

## 6. 阅读时间估算汇总

| 优先级 | 模块数 | 总代码行数 | 总预估时间 | 精读要求 |
|--------|--------|-----------|-----------|---------|
| P0 | 6（含 CPI 实现） | ~13,546 | 18–25h | 逐行 + 公式对照 |
| P1 | 4 | ~9,480 | 3–4h | 理解策略 |
| P2 | 2 | ~4,847 | 0.5h | 了解接口 |
| P3 | 5* | ~8,397 + 数据 | 按需 | 查函数签名 |

> *P3 模块数为 5（含 ov_data 非代码）；行数仅核心 `ov_*` 源码，未计 ov_eval 全部工具与 sim/plot 细项。
> **核心精读工作量集中在 P0（约 20h），P1–P3 合计约 4h**，符合"算法密集、工程薄"的特征。

---

## 7. 推荐阅读路线（细化到函数级）

> 下列顺序按"依赖关系"排列：每步用到的类型/工具，已在前面步骤读完。标号 `S1…S17` 为精读顺序，`(文件:行)` 为入口锚点。

### 阶段 A — 状态表示基础（P0-5）
```
S1  Type 基类            ov_core/src/types/Type.h:37
    └─ 读 _fej / set_value / set_fej / value() / check_if_same  —— FEJ 机制的总开关
S2  JPLQuat              ov_core/src/types/JPLQuat.h:92
    └─ JPL 四元数定义、Rot()/q_FEJ、误差状态维度（配 00 资料 [40] Trawny 2005）
S3  PoseJPL              ov_core/src/types/PoseJPL.h:37
    └─ 6-DOF 位姿 = JPLQuat + Vec3；set_value_internal/set_fej_internal（FEJ 落地）
S4  IMU / Vec / Landmark ov_core/src/types/{IMU,PoseJPL,Vec,Landmark}.h
    └─ IMU(Rot/pos/vel/bias 组合)、Landmark 的 anchored 表示 + FEJ 选择（配 [20][27]）
```
> **A 阶段产出**：理解"每个 Type 同时持有 value 与 fej，更新只动 value、雅可比用 fej"——这是全仓一致性的根。

### 阶段 B — 预测（P0-1）
```
S5  Propagator::propagate_and_clone   ov_msckf/src/state/Propagator.cpp:33
    └─ 主入口：选段→逐段预测→累加 Phi/Qd→前推均值→augment_clone
S6  Propagator::select_imu_readings   Propagator.cpp:269   （时间窗选取+插值）
S7  Propagator::predict_and_compute   Propagator.cpp:395   （F/G 计算分发）
S8  predict_mean_discrete / _rk4 / _analytic + compute_F_and_G_* / compute_Xi_sum
    Propagator.cpp:482 / :507 / :667 / :683 / :830 / :588
    └─ 三条积分路径；确认运行时走哪条（配 00 资料 [12][13][44]）
```
> **B 阶段产出**：IMU 预积分 + 状态克隆（MSCKF 滑动窗口的"窗口"由此生长）。

### 阶段 C — 状态增广与边缘化（P0-4）
```
S9  StateHelper::EKFPropagation / EKFUpdate   StateHelper.cpp:36 / :116
    └─ 协方差前向传播 + 通用 EKF 更新（被 B/S10/S11 复用）
S10 StateHelper::augment_clone                StateHelper.cpp:579   （窗口增广）
S11 StateHelper::marginalize / marginalize_old_clone / marginalize_slam
    StateHelper.cpp:271 / :618 / :631          （边缘化 / 滑窗 / SLAM 路标修剪）
S12 StateHelper::initialize / initialize_invertible   StateHelper.cpp:393 / :484
    └─ 新变量（SLAM 路标）加入状态
```
> **C 阶段产出**：滑窗的"增—边"闭环，紧接 B 之后读，正好对应 propagate_and_clone 里的 augment 调用。

### 阶段 D — 测量更新（P0-2 / P0-3）
```
S13 UpdaterMSCKF::update        ov_msckf/src/update/UpdaterMSCKF.cpp:58
    └─ 多帧投影残差 + MSCKF 边缘化（H 矩阵、nullspace 投影）（配 [32]）
S14 UpdaterSLAM::update         ov_msckf/src/update/UpdaterSLAM.cpp:253
    └─ SLAM 特征 3D 点重投影残差；change_anchors/perform_anchor_change (:481/:505)
S15 UpdaterSLAM::delayed_init   UpdaterSLAM.cpp:61   （轨迹达标→升格 Landmark）
S16 UpdaterZeroVelocity::try_update  ov_msckf/src/update/UpdaterZeroVelocity.cpp
    └─ 静止检测下的零速约束（并入 D 阶段残差框架看）
```
> **D 阶段产出**：滤波器"更新"柱，与 B/C 构成完整 MSCKF+SLAM 闭环。

### 阶段 E — 主循环编排（P1-1 → P1-2/3/4）
```
S17 VioManager::feed_measurement_imu → track_image_and_update → do_feature_propagate_update
    ov_msckf/src/core/VioManager.cpp:166 / :256 / :323
    └─ 把 B→C→D 串起来；do_feature_propagate_update 内做 feats_lost/marg/slam 分组
    └─ 配套 P1-2 FeatureDatabase(特征选取)、P1-3 TrackKLT(前端)、P1-4 ZUPT 触发
```

### 阶段 F — 初始化（P0-6，可独立于 A–E 成块）
```
F1  InertialInitializer::feed_imu / initialize   ov_init/src/init/InertialInitializer.cpp:49 / :73
    └─ 调度三种策略
F2  StaticInitializer / DynamicInitializer::initialize
    ov_init/src/dynamic/DynamicInitializer.cpp:44
    └─ DynamicInitializer 内构造 ov_core::CpiV1 预积分器 (:243-262)
       + ov_init/src/ceres/Factor_ImuCPIv1.* 接入 Ceres MLE 对齐（配 [11]）
F3  ov_core/src/cpi/CpiV1.*  —— 作为 F2 的实现细节精读（见 P0-6 更正说明）
```
> **F 阶段产出**：冷启动得到初始位姿/速度/bias/尺度，之后才进入 A–E 主循环。

### 阶段 G — 工程支撑（P2 / P3，用到时查）
```
G1  ROS I/O（P2-1）：ROS1/ROS2Visualizer、Subscriber —— 仅看话题名/参数
G2  相机模型（P3-1）：Camera/CameraRadtan/CameraFishEye —— 投影/反投影接口
G3  CPI 在线侧对照（P3-2）：Propagator 未 include，仅比对思想
G4  utils / sim / plot / ov_eval / ov_data —— 按需
```

---

### 路线总览（依赖箭头）

```
A(类型/FEJ) ──► B(预测/克隆) ──► C(增广/边缘化) ──► D(更新 MSCKF/SLAM/ZUPT)
                                                      │
E(主循环 VioManager 串联 B→C→D + 前端/特征库) ◄────────┘
                                                      │
F(初始化: CpiV1+Ceres MLE，独立成块，冷启动先行或后置) ┘

G(ROS I/O · 相机模型 · CPI对照 · 工具)  —— 横切，按需查
```

> **阅读策略建议**：
> 1. 严格按 A→B→C→D→E 顺序，每一阶段配齐对应 citelist 论文（已在各步标注）。
> 2. **F（初始化）可提前**：若你更关心"系统如何启动"，可在 B 之后跳到 F（F2 直接用到 CpiV1，已并入 P0-6）。
> 3. **CPI 双重性**：F 阶段把 `CpiV1` 当实现读；B 阶段遇预积分公式分歧时，回 G3 对照 CPI 思想。
> 4. 详细的逐函数精读（含公式对照、调用细节）在 **Phase 4（核心模块深读）** 中展开，本清单 S1–S17 + F1–F3 即为其精读入口清单。

---

## 附录 A：关键词 grep 命中（验证用）

| 模块 | propagate | residual/factor | marginal/FEJ | initialize/gravity | 位置判定 |
|------|-----------|-----------------|--------------|--------------------|---------|
| Propagator | 34 | ✓ | ✓(clone) | — | P0 |
| UpdaterMSCKF | ✓ | 12 | ✓ | — | P0 |
| UpdaterSLAM | ✓ | 23 | ✓ | — | P0 |
| UpdaterZeroVelocity | ✓ | 18 | — | ✓(静止) | P0/P1 |
| StateHelper | ✓ | — | 42(marginal/clone) | — | P0 |
| types/* | — | — | FEJ 全类型 | — | P0 |
| DynamicInitializer | ✓ | — | — | 72 | P0 |
| VioManager | ✓ | ✓(调用) | — | ✓ | P1 |
| FeatureDatabase | — | — | — | —(feature) | P1 |
| TrackKLT | — | — | — | —(track) | P1 |
| ROS Visualizer | — | — | — | —(publish) | P2 |

> ZUPT 含残差更新逻辑（P0 数学），但其在 pipeline 中属"条件触发的可选更新"，整体归 P1（策略模块），精读时可并入 P0-2/3 的残差框架一起看。
